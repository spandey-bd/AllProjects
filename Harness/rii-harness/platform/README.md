# Platform specific code and project files

Each platform (e.g. iOS, android, etc) should have its own subdirectory here where anything specific to that platform (including IDE project files) should reside.
